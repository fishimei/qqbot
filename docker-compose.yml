services:
  bot:
    image:
      fishimei/qqbot:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8077:8077"
    volumes:
      - ./config.yaml:/app/config.yaml
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped